PORT=5001
NODE_ENV=production
TEMP_DIR=/tmp/oj-jobs

# Must match the EXECUTION_SERVICE_TOKEN in the backend .env and in AWS SSM Parameter Store
EXECUTION_SERVICE_TOKEN=replace_with_same_token_as_backend
